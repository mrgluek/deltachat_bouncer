services:
  bot:
    build: .
    container_name: deltachat_bouncer
    restart: always
    volumes:
      - ./data:/app/data
      - ./data/bouncer:/root/.config/bouncer
    environment:
      - DC_DB_DIR=/app/data
      - DB_PATH=/app/data/bouncer.db
      - TZ=UTC
